spring:
  application:
    name: auth_service
  datasource:
    url: jdbc:mysql://mysql:3306/academia_noafer
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:

    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  liquibase:
    enabled: false

server:
  port: 8087

jwt:
  secret: esta_es_mi_compleja_clave_secreta_para_jwt_2026_academia_noafer
  expiration: 3600000

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    try-it-out-enabled: true
    operations-sorter: method
    tags-sorter: alpha
    display-request-duration: true
